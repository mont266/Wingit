
import React from 'react';

// This component was removed as it was unused and its dependency (react-simple-maps)
// was causing a conflict with the React version. This file can be safely deleted.

const FlightMap: React.FC = () => {
  return null;
};

export default FlightMap;
